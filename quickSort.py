def partition(part, pivot):
	# returns left & right

def quickSort(p):
	# Limiting case

	# other than last, last element
	left, right = partition(p[:-1], p[-1])

	# Joined the elements
	p = left + p + right